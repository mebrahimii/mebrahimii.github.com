public class MultiAssert {
    public static int returnParam(int param) {
        return param;
    }

    // No main defined.
    // We cannot run this program as-is, but we can test it
    // (see MultiAssertTest.java)
}
